/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
    domains: [
        "fiverr-res.cloudinary.com", "res.cloudinary.com",
        "res.cloudinary.com"
    ]
}
};

export default nextConfig;
